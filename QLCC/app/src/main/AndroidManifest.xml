<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QLCC">
        <activity android:name=".HomeUserActivity" />
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".ResidentActivity" />
        <activity android:name=".AdminAnnouncementActivity" />
        <activity
            android:name=".AdminApartmentActivity"
            android:exported="false" />
        <activity android:name=".ReportActivity" />
        <activity android:name=".ReportHistoryActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".AdminInvoiceActivity" />
        <activity android:name=".AdminBillEditorActivity" />
        <activity android:name=".UserInvoiceActivity" />

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.qlcc.VehicleActivity" />

    </application>
</manifest>
